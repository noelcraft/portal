/*
  This is the configuration file, you can
  customize the value by yourself. Do not
  replace the default ones at the bottom.

  Keep this to the default if you want to
  play multiplayer, or every player has to
  use the same configuration.
*/

module.exports = {
  /*
    Whether or not show the portal indicator for the
    current portal color. Set to false to turn it off
    or set to true to turn it on.
  */
  showIndicator: true,

  /*
    Key bindings to switch between portal colors. Only
    uppercase A-Z are used. For computer players only.
  */
  switchBlue: "I",
  switchOrange: "O"
}
